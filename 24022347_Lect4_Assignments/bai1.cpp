// Hai tính chất quan trọng của một hàm băm là:

// 1. Tính xác định (Deterministic):

// Cùng một đầu vào (input) luôn cho ra cùng một đầu ra (output) duy nhất. Điều này đảm bảo rằng nếu bạn băm một giá trị nhiều lần, kết quả luôn giống nhau.

// 2. Tính chống va chạm (Collision Resistance):

// Rất khó để tìm được hai giá trị đầu vào khác nhau nhưng cho ra cùng một giá trị băm. Điều này giúp ngăn chặn các cuộc tấn công dựa trên việc tạo ra hai đầu vào khác nhau có cùng giá trị băm.